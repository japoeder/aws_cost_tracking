{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run runCostReporting.py",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/runCostReporting.py",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}